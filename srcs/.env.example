# Local Docker Compose defaults. Replace secrets before sharing a deployed environment.
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
POSTGRES_DB=miniexchange

APP_REPORT_PATH=reports
RUN_TESTS=false
JWT_SECRET=replace-this-local-development-secret

MATCHING_ENGINE_HOST=matching-engine
MATCHING_ENGINE_PORT=50051
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
TRADE_EVENTS_TOPIC=trade-events
TRADE_EVENTS_CONSUMER_GROUP=mini-exchange-trades
