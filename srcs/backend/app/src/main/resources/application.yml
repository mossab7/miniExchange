spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/miniexchange
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

    show-sql: true

jwt.secret: ${JWT_SECRET}